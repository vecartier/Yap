---
phase: 01-foundation
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - OpenOats/Sources/OpenOats/Intelligence/KnowledgeBase.swift
  - OpenOats/Sources/OpenOats/Intelligence/SuggestionEngine.swift
  - OpenOats/Sources/OpenOats/Intelligence/VoyageClient.swift
  - OpenOats/Sources/OpenOats/Intelligence/OllamaEmbedClient.swift
  - OpenOats/Sources/OpenOats/Views/SuggestionsView.swift
  - OpenOats/Tests/OpenOatsTests/KnowledgeBaseTests.swift
  - OpenOats/Sources/OpenOats/Models/Models.swift
  - OpenOats/Sources/OpenOats/Settings/AppSettings.swift
  - OpenOats/Sources/OpenOats/App/AppRuntime.swift
  - OpenOats/Sources/OpenOats/Views/ContentView.swift
  - OpenOats/Sources/OpenOats/Storage/SessionStore.swift
  - OpenOats/Sources/OpenOats/Views/SettingsView.swift
autonomous: true
requirements:
  - CLEAN-01
  - CLEAN-02
  - CLEAN-03

must_haves:
  truths:
    - "Project builds with zero errors after KB removal"
    - "No KB-related types (KBResult, Suggestion, SuggestionEngine, EmbeddingProvider) exist in the compiled target"
    - "Settings screen shows no Knowledge Base or Embedding Provider sections"
    - "ContentView shows no suggestions panel or KB indexing progress indicator"
    - "appendRecordDelayed compiles without suggestionEngine parameter"
  artifacts:
    - path: "OpenOats/Sources/OpenOats/Intelligence/KnowledgeBase.swift"
      provides: "deleted"
    - path: "OpenOats/Sources/OpenOats/Intelligence/SuggestionEngine.swift"
      provides: "deleted"
    - path: "OpenOats/Sources/OpenOats/Intelligence/VoyageClient.swift"
      provides: "deleted"
    - path: "OpenOats/Sources/OpenOats/Intelligence/OllamaEmbedClient.swift"
      provides: "deleted"
    - path: "OpenOats/Sources/OpenOats/Views/SuggestionsView.swift"
      provides: "deleted"
    - path: "OpenOats/Tests/OpenOatsTests/KnowledgeBaseTests.swift"
      provides: "deleted"
    - path: "OpenOats/Sources/OpenOats/Models/Models.swift"
      provides: "KB types removed, ConversationState stripped to core fields"
    - path: "OpenOats/Sources/OpenOats/Settings/AppSettings.swift"
      provides: "EmbeddingProvider enum and KB settings properties removed, Keychain migration added"
    - path: "OpenOats/Sources/OpenOats/App/AppRuntime.swift"
      provides: "AppServices struct contains no KB/SE entries"
    - path: "OpenOats/Sources/OpenOats/Views/ContentView.swift"
      provides: "KB state vars removed, SuggestionsView reference removed"
    - path: "OpenOats/Sources/OpenOats/Storage/SessionStore.swift"
      provides: "appendRecordDelayed has no suggestionEngine parameter"
    - path: "OpenOats/Sources/OpenOats/Views/SettingsView.swift"
      provides: "Knowledge Base and Embedding Provider sections removed"
  key_links:
    - from: "AppRuntime.AppServices"
      to: "TranscriptionEngine / TranscriptLogger / etc."
      via: "makeServices() factory — KB entries removed"
      pattern: "struct AppServices \\{[^}]*transcriptionEngine"
    - from: "SessionStore.appendRecordDelayed"
      to: "TranscriptStore (refined text backfill)"
      via: "delay + transcriptStore parameter only"
      pattern: "func appendRecordDelayed.*transcriptStore"
---

<objective>
Remove the knowledge base feature entirely from the codebase — delete four Intelligence files plus the test file and SuggestionsView, then surgically excise all KB references from Models.swift, AppSettings.swift, AppRuntime.swift, ContentView.swift, SessionStore.swift, and SettingsView.swift.

Purpose: Strip dead code and remove a capability that is explicitly out of scope, so Phase 2 and 3 work on a clean foundation without KB noise or compile-time coupling.
Output: A building codebase with zero KB symbols, no suggestion UI, and no embedding settings.
</objective>

<execution_context>
@/Users/vcartier/.claude/get-shit-done/workflows/execute-plan.md
@/Users/vcartier/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-foundation/01-CONTEXT.md
@.planning/phases/01-foundation/01-RESEARCH.md

<interfaces>
<!-- Key interfaces the executor needs to understand before making changes. -->

From OpenOats/Sources/OpenOats/Models/Models.swift (lines 56-78, KEEP these):
```swift
// MARK: - Conversation State
struct ConversationState: Sendable, Codable {
    var currentTopic: String
    var shortSummary: String
    var openQuestions: [String]
    var activeTensions: [String]
    var recentDecisions: [String]
    var themGoals: [String]                      // REMOVE
    var suggestedAnglesRecentlyShown: [String]   // REMOVE
    var lastUpdatedAt: Date
}
```

From OpenOats/Sources/OpenOats/App/AppRuntime.swift (lines 15-22):
```swift
struct AppServices {
    let knowledgeBase: KnowledgeBase       // DELETE
    let suggestionEngine: SuggestionEngine // DELETE
    let transcriptionEngine: TranscriptionEngine
    let transcriptLogger: TranscriptLogger
    let refinementEngine: TranscriptRefinementEngine
    let audioRecorder: AudioRecorder
}
```

From OpenOats/Sources/OpenOats/Storage/SessionStore.swift (method to simplify):
```swift
// Current signature (must become):
// func appendRecordDelayed(utteranceID: UUID, transcriptStore: TranscriptStore) async
// Remove: suggestionEngine: SuggestionEngine? parameter
// Remove: decision/latestSuggestion capture from method body
```

From OpenOats/Sources/OpenOats/App/AppCoordinator.swift (startTranscription — unchanged, for reference):
```swift
private func startTranscription(metadata: MeetingMetadata, settings: AppSettings?) async {
    // ...
    await transcriptionEngine?.start(
        locale: settings.locale,
        inputDeviceID: settings.inputDeviceID,
        transcriptionModel: settings.transcriptionModel
    )
}
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Delete KB files and test file</name>
  <files>
    OpenOats/Sources/OpenOats/Intelligence/KnowledgeBase.swift,
    OpenOats/Sources/OpenOats/Intelligence/SuggestionEngine.swift,
    OpenOats/Sources/OpenOats/Intelligence/VoyageClient.swift,
    OpenOats/Sources/OpenOats/Intelligence/OllamaEmbedClient.swift,
    OpenOats/Sources/OpenOats/Views/SuggestionsView.swift,
    OpenOats/Tests/OpenOatsTests/KnowledgeBaseTests.swift
  </files>
  <read_first>
    - OpenOats/Sources/OpenOats/Intelligence/KnowledgeBase.swift (understand what imports to confirm safe deletion)
    - OpenOats/Sources/OpenOats/Intelligence/SuggestionEngine.swift
    - OpenOats/Sources/OpenOats/Intelligence/VoyageClient.swift
    - OpenOats/Sources/OpenOats/Intelligence/OllamaEmbedClient.swift
    - OpenOats/Sources/OpenOats/Views/SuggestionsView.swift
    - OpenOats/Tests/OpenOatsTests/KnowledgeBaseTests.swift
  </read_first>
  <action>
Delete all six files using the filesystem. These files have no consumers outside themselves after the changes in Task 2 are applied — they import only Foundation and each other.

Files to delete (absolute paths):
- /Users/vcartier/Desktop/OpenOats-fork/OpenOats/Sources/OpenOats/Intelligence/KnowledgeBase.swift
- /Users/vcartier/Desktop/OpenOats-fork/OpenOats/Sources/OpenOats/Intelligence/SuggestionEngine.swift
- /Users/vcartier/Desktop/OpenOats-fork/OpenOats/Sources/OpenOats/Intelligence/VoyageClient.swift
- /Users/vcartier/Desktop/OpenOats-fork/OpenOats/Sources/OpenOats/Intelligence/OllamaEmbedClient.swift
- /Users/vcartier/Desktop/OpenOats-fork/OpenOats/Sources/OpenOats/Views/SuggestionsView.swift
- /Users/vcartier/Desktop/OpenOats-fork/OpenOats/Tests/OpenOatsTests/KnowledgeBaseTests.swift

Note: The project uses FileSystemSynchronizedRootGroup — file deletion from disk is sufficient, no manual Xcode project edits needed.
  </action>
  <verify>
    <automated>ls /Users/vcartier/Desktop/OpenOats-fork/OpenOats/Sources/OpenOats/Intelligence/ 2>&1</automated>
  </verify>
  <acceptance_criteria>
    - `ls OpenOats/Sources/OpenOats/Intelligence/` returns no KnowledgeBase.swift, SuggestionEngine.swift, VoyageClient.swift, OllamaEmbedClient.swift
    - `ls OpenOats/Sources/OpenOats/Views/` returns no SuggestionsView.swift
    - `ls OpenOats/Tests/OpenOatsTests/` returns no KnowledgeBaseTests.swift
  </acceptance_criteria>
  <done>All six files absent from disk.</done>
</task>

<task type="auto">
  <name>Task 2: Scrub KB references from all remaining source files</name>
  <files>
    OpenOats/Sources/OpenOats/Models/Models.swift,
    OpenOats/Sources/OpenOats/Settings/AppSettings.swift,
    OpenOats/Sources/OpenOats/App/AppRuntime.swift,
    OpenOats/Sources/OpenOats/Views/ContentView.swift,
    OpenOats/Sources/OpenOats/Storage/SessionStore.swift,
    OpenOats/Sources/OpenOats/Views/SettingsView.swift
  </files>
  <read_first>
    - OpenOats/Sources/OpenOats/Models/Models.swift (full file — contains KB types after line 80)
    - OpenOats/Sources/OpenOats/Settings/AppSettings.swift (full file — EmbeddingProvider at line 108, KB properties scattered)
    - OpenOats/Sources/OpenOats/App/AppRuntime.swift (full file — AppServices struct at line 15)
    - OpenOats/Sources/OpenOats/Views/ContentView.swift (full file — KB state vars, SuggestionsView usage, ViewState KB fields)
    - OpenOats/Sources/OpenOats/Storage/SessionStore.swift (full file — appendRecordDelayed signature)
    - OpenOats/Sources/OpenOats/Views/SettingsView.swift (full file — KB and Embedding sections)
  </read_first>
  <action>
Make surgical edits to each file:

**Models.swift:**
- Delete the entire block from `// MARK: - Suggestion Trigger` through the closing brace of `struct Suggestion` (lines 80-179). This removes: `SuggestionTriggerKind`, `SuggestionTrigger`, `SuggestionEvidence`, `SuggestionDecision`, `SuggestionFeedback`, `KBResult`, `Suggestion`.
- In `ConversationState`: remove the two KB-only fields `var themGoals: [String]` and `var suggestedAnglesRecentlyShown: [String]`.
- In `ConversationState.empty`: remove the `themGoals: []` and `suggestedAnglesRecentlyShown: []` initializer arguments.
- In `SessionRecord` (if it exists in this file): remove fields `suggestions`, `kbHits`, `suggestionDecision`, `surfacedSuggestionText`.

**AppSettings.swift:**
- Remove `enum EmbeddingProvider: String, CaseIterable, Identifiable { ... }` (the entire enum block starting at line 108).
- Remove these `@ObservationIgnored` property blocks and their computed vars: `_kbFolderPath` / `kbFolderPath`, `_voyageApiKey` / `voyageApiKey`, `_embeddingProvider` / `embeddingProvider`, `_ollamaEmbedModel` / `ollamaEmbedModel`, `_openAIEmbedBaseURL` / `openAIEmbedBaseURL`, `_openAIEmbedApiKey` / `openAIEmbedApiKey`, `_openAIEmbedModel` / `openAIEmbedModel`.
- Remove the `kbFolderURL` computed property.
- In `init`: remove assignment lines for all KB/embed properties (`_kbFolderPath`, `_voyageApiKey`, `_embeddingProvider`, `_ollamaEmbedModel`, `_openAIEmbedBaseURL`, `_openAIEmbedApiKey`, `_openAIEmbedModel`).
- In migration functions (runMigration3, runMigration4, etc.): remove "kbFolderPath", "embeddingProvider", "ollamaEmbedModel" from UserDefaults reset arrays. Remove "voyageApiKey" from Keychain reset arrays.
- Add a new migration step (find the highest existing `runMigrationN` and add `runMigrationN+1`) that deletes the stale Keychain entries: call `secretStore.delete(key: "voyageApiKey")` and `secretStore.delete(key: "openAIEmbedApiKey")`. Register this migration in the migration runner (the same pattern used by existing migrations).

**AppRuntime.swift:**
- In `struct AppServices`: remove `let knowledgeBase: KnowledgeBase` and `let suggestionEngine: SuggestionEngine`.
- In `makeServices()` (or equivalent factory): remove KB and SuggestionEngine construction and the lines that add them to the services struct.
- In `ensureServicesInitialized()` or equivalent: remove any `.knowledgeBase` and `.suggestionEngine` references.
- Remove any `import` statements that were only needed for KB types.

**ContentView.swift:**
- Remove `@State private var knowledgeBase: KnowledgeBase?` and `@State private var suggestionEngine: SuggestionEngine?`.
- In `ViewState`: remove fields `kbIndexingProgress`, `suggestions`, `isGeneratingSuggestions`, `kbFolderPath`, `voyageApiKey`.
- Remove `@State private var observedKBFolderPath`, `@State private var observedVoyageApiKey`.
- In `rootContent`: remove the entire "SUGGESTIONS" `VStack` block (lines 145-157 per current content, including the `SuggestionsView(...)` call and its label/divider).
- In `rootContent`: remove the `if !viewState.kbIndexingProgress.isEmpty { ... }` status text from the header HStack.
- Remove `indexKBIfNeeded()` method entirely.
- In `startSession()`: remove `suggestionEngine?.clear()`.
- Remove the suggestion-related call to `appendRecordDelayed` that passes `suggestionEngine:`.
- In `synchronizeDerivedState()`: remove KB/suggestion-related observation blocks.
- In `handlePendingExternalCommandIfPossible()` guard: remove suggestion-related guard conditions.

**SessionStore.swift:**
- Remove `suggestionEngine: SuggestionEngine?` parameter from `appendRecordDelayed`.
- Remove the `decision` and `latestSuggestion` capture inside the method body.
- Update all call sites of `appendRecordDelayed` within SessionStore itself (if any) to drop the parameter.
- Note: After this change, `appendRecordDelayed` signature becomes: `func appendRecordDelayed(utteranceID: UUID, transcriptStore: TranscriptStore) async` (or whatever remains after removing the KB parameter — retain all non-KB parameters).

**SettingsView.swift:**
- Remove the `Section { ... } header: { Text("Knowledge Base") }` block entirely.
- Remove the `Section { ... } header: { Text("Embedding Provider") }` block entirely (or equivalent picker for embedding provider selection).
- Remove the `chooseKBFolder()` method.
- Remove any `import` lines that were only needed for KB-specific UI types.

After all edits, run the build to confirm zero errors before proceeding:
`cd /Users/vcartier/Desktop/OpenOats-fork/OpenOats && swift build 2>&1 | tail -20`

If there are errors, fix them before considering the task done. Common follow-up fixes:
- Any remaining call sites of `appendRecordDelayed(... suggestionEngine:)` in ContentView.swift — remove the argument label and argument.
- Any `SessionRecord` fields referencing `Suggestion` or `KBResult` in Models.swift if they were missed.
  </action>
  <verify>
    <automated>cd /Users/vcartier/Desktop/OpenOats-fork/OpenOats && swift build 2>&1 | grep -E "^.*error:" | wc -l | tr -d ' '</automated>
  </verify>
  <acceptance_criteria>
    - `swift build 2>&1 | grep "error:" | wc -l` outputs `0`
    - `grep -r "KnowledgeBase\|SuggestionEngine\|VoyageClient\|OllamaEmbedClient\|EmbeddingProvider\|KBResult\|SuggestionDecision" OpenOats/Sources/ 2>/dev/null` returns no matches
    - `grep -r "kbFolderPath\|voyageApiKey\|embeddingProvider\|ollamaEmbedModel\|openAIEmbedModel" OpenOats/Sources/OpenOats/Settings/AppSettings.swift 2>/dev/null` returns no matches
    - `grep "SuggestionsView" OpenOats/Sources/OpenOats/Views/ContentView.swift 2>/dev/null` returns no matches
    - `grep "knowledgeBase\|suggestionEngine" OpenOats/Sources/OpenOats/App/AppRuntime.swift 2>/dev/null` returns no matches
    - `grep "Knowledge Base\|Embedding Provider\|chooseKBFolder" OpenOats/Sources/OpenOats/Views/SettingsView.swift 2>/dev/null` returns no matches
    - `grep "suggestionEngine" OpenOats/Sources/OpenOats/Storage/SessionStore.swift 2>/dev/null` returns no matches
  </acceptance_criteria>
  <done>
    Project builds clean (zero errors). grep confirms no KB symbols remain in any source file. Settings screen has no KB or embedding sections. ContentView has no suggestion UI.
  </done>
</task>

</tasks>

<verification>
After both tasks complete:

```bash
cd /Users/vcartier/Desktop/OpenOats-fork/OpenOats && swift build 2>&1 | grep "error:" | wc -l
# Expected: 0

grep -rn "KnowledgeBase\|SuggestionEngine\|EmbeddingProvider\|VoyageClient\|OllamaEmbedClient" OpenOats/Sources/ 2>/dev/null
# Expected: no output

cd /Users/vcartier/Desktop/OpenOats-fork/OpenOats && swift test --filter MeetingStateTests 2>&1 | tail -5
# Expected: tests pass (KB removal doesn't touch meeting state tests)
```
</verification>

<success_criteria>
- `swift build` exits with 0 errors
- Six KB files are absent from disk
- `grep -r "SuggestionEngine\|KnowledgeBase\|EmbeddingProvider" OpenOats/Sources/` returns no matches
- `grep "suggestionEngine" OpenOats/Sources/OpenOats/Storage/SessionStore.swift` returns no matches
- `MeetingStateTests` suite still passes
</success_criteria>

<output>
After completion, create `.planning/phases/01-foundation/01-01-SUMMARY.md`
</output>
